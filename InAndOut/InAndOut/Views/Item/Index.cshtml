@model IEnumerable<Item>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<div class="row">
    <div class="col-6">
        <a class="btn btn-danger" asp-action="Index" asp-controller="Home">Delete Item</a>
    </div>
    <div class="col-6">
        @*<a class="btn btn-primary" asp-action="Index" asp-controller="Home">Create New Item</a>*@
        <button class="btn btn-secondary btn-outline-dark text-white">Ez egy gomb</button>
    </div>
</div>
@foreach (var item in Model)
{
    <h1>@item.Borrower</h1>
}

